- Yes , arrays.
- const a = \[] is not a array.
- Javascript arrays are cool , do a lot of stuff under the hood.
- Contiguous memory space with certain amount of bytes.
	- Computer treats the 4 bytes of memory as a singular number -> does not have a specific meaning, only for a humans to understand the memory here.
	- int\[3] -> 3 * type_size(32bits/4bytes) memory allocated together ; an array.
	- For us it is like a slot, under the hood discrete memory .
- Insertion:
	- dont append into it, rather we replace the item in memory.
	- we cannot grow a array simply, list can grow.
	- capacity -> make a optimized way to use memory without reallocation.
	- arr + width * offset 
	- dont delete anything ; set the memory to null.(named value for not something at that memory location)
- TC to get an item from an array:
	- does it grow with input ?
	- yes ! O(1) -> constant amount of things , doesnt grow with input size. dont have to walk to there as we know the offset(index).
	- all are instant operation.
- no methods on traditional method on arrays.
- if arr does not grow, we have to give the size beforehand we have to grow the array. compile times arrays to grow its array. It has to have the size as part of its allocation.
- in javascript, they turn it into an map once it is too many items or sparsed.
